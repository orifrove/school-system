"""
Development settings.

Запускается через DJANGO_SETTINGS_MODULE=config.settings.dev
(см. .env.example — это значение по умолчанию для локальной разработки).
"""

from .base import *  # noqa: F401,F403

DEBUG = True

ALLOWED_HOSTS = ["localhost", "127.0.0.1"]

# Более подробный вывод SQL/логов в консоль во время разработки — по желанию,
# не включаем по умолчанию, чтобы не заваливать вывод; включить точечно,
# если понадобится отладка конкретного запроса.
